var cars = ["Mustang", "Corvette", "Jaguar"];


let length = cars.push("Tesla"); //add an element at last

console.log(cars)
console.log(length)

// The push() method adds one or more elements to the end of an array and returns the new length of the array.
